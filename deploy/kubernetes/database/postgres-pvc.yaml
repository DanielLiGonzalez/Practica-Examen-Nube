apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: cafe-boreal
  labels:
    app: postgres
    app.kubernetes.io/part-of: cafe-boreal
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
